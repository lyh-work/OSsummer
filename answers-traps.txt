1. RISC-V 中一共有 a0~a7 一共 8 个寄存器。在 main 函数调用 printf 函数的过程中，参数 13 被存储在了寄存器 a2 中。
2. 对于函数 f 调用是直接计算出了结果 12， 对于函数 g 的调用则是内联在了函数 f 中。
3. 0x638
4. 0x38
5. 上述代码输出为`HE110 World`。如果为大端法，i需要被更改为`0x726c6400`，57616不需要被改变，硬件会处理具体的存储和读取方式。
6. 会打印出 `a2` 寄存器当前的值，因为在调用 `printf` 时，并未给 `a2` 寄存器赋值，但在`printf`被执行的时候却无法检查当前 `a2` 中的值是否为用户传入的值，而只是单纯的对其进行打印。